[project]
name = "autonomous-executive-assistant-env"
version = "0.1.0"
description = "Executive assistant agent"
authors = [{name = "Your Name"}]
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
